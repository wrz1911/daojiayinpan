# 统一行尾策略: 任何平台(Windows/Linux/macOS)克隆与构建都得到逐字节相同的文本文件
#
# 为什么必须显式声明: qimen_bundle.min.js / yinpan_app.min.css 是**入库的构建产物**,
# CI 会重建并与仓库内容逐字节比对(git diff --exit-code)。若 Windows 侧按 CRLF 检出、
# 构建出的产物却含 LF 换行, 每次本地构建都会在 git status 里留下假修改, 且 CI 校验
# 的稳定性依赖运行平台的 autocrlf 默认值 —— 显式声明后不再依赖任何本地 git 配置。
* text=auto eol=lf

# 二进制资源: 禁止行尾转换(Git 虽可自动识别, 显式声明避免误判)
*.png binary
*.ico binary
*.icns binary
*.keystore binary
*.svg text
